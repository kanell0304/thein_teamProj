package com.anything.momeogji.repository;

import com.anything.momeogji.entity.recommendation.FinalNoticeChangeLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinalNoticeChangeLogRepository extends JpaRepository<FinalNoticeChangeLog, Long> {
}
